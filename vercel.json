{
  "rewrites": [
    { "source": "/sitemap.xml", "destination": "/api/sitemap" },
    { "source": "/api/:path*",  "destination": "/api/:path*"  }
  ],
  "functions": {
    "api/*.js": {
      "maxDuration": 60
    }
  }
}
